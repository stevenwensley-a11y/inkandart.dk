<article class="card card--red card--tilt-l" aria-labelledby="contact-heading">
  <div class="card__file">// KONTAKT.DUMP</div>
  <h2 id="contact-heading" class="visually-hidden">Kontakt</h2>

  <ul class="contact__list">
    <li>
      <span aria-hidden="true">📞</span>
      <a href="tel:{{ site.phoneIntl }}">{{ site.phone }}</a>
    </li>
    <li>
      <span aria-hidden="true">💬</span>
      <a href="https://wa.me/{{ site.whatsapp | replace('+', '') | replace(' ', '') }}" rel="noopener">WHATSAPP</a>
    </li>
    <li>
      <span aria-hidden="true">✉</span>
      <a href="mailto:{{ site.email }}">{{ site.email | upper }}</a>
    </li>
  </ul>

  <a href="{{ site.bookingUrl }}" class="btn btn--ink" rel="noopener">Book tid →</a>
</article>

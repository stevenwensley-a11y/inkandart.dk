{% set lang = lang or (page.url | pageLang) %}
<article class="card card--red card--tilt-l" aria-labelledby="contact-heading">
  <div class="card__file">// {% if lang == "en" %}CONTACT.DUMP{% else %}KONTAKT.DUMP{% endif %}</div>
  <h2 id="contact-heading" class="visually-hidden">{% if lang == "en" %}Contact{% else %}Kontakt{% endif %}</h2>

  <ul class="contact__list">
    <li>
      <span aria-hidden="true">📞</span>
      <a href="tel:{{ site.phoneIntl }}">{{ site.phone | replace(" ", "&nbsp;") | safe }}</a>
    </li>
    <li>
      <span aria-hidden="true">💬</span>
      <a href="https://wa.me/{{ site.whatsapp | replace('+', '') | replace(' ', '') }}" target="_blank" rel="noopener noreferrer">WHATSAPP</a>
    </li>
    <li>
      <span aria-hidden="true">◎</span>
      <a href="{{ site.instagram }}" target="_blank" rel="noopener noreferrer">INSTAGRAM</a>
    </li>
  </ul>

  <a href="{{ site.bookingUrl }}" class="btn btn--ink" target="_blank" rel="noopener noreferrer">{{ "cta.bookNow" | t(lang) }} <span aria-hidden="true">→</span></a>
</article>
